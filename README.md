<div align="center">
    <h1>CLog</h1>
    <p>(cool log)</p>
</div>

---

# Source Tree

The actual library itself is within the `include` folder. Testing / example source code can be found within the `source` folder. To include CLog in your project you can copy paste the `include` folder into your project and add it to your include directories in your project. 